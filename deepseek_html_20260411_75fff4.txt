<!DOCTYPE html>
<html lang="ru">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Админ-панель | Океан праздников</title>
  <link href="https://fonts.googleapis.com/css2?family=Inter:opsz,wght@14..32,300;400;500;600;700;800&display=swap" rel="stylesheet">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">
  <style>
    * { margin: 0; padding: 0; box-sizing: border-box; }
    body { font-family: 'Inter', sans-serif; background: #f1f5f9; padding: 40px 20px; }
    .container { max-width: 1000px; margin: 0 auto; }
    .login-box, .admin-panel { background: white; border-radius: 48px; padding: 40px; box-shadow: 0 25px 50px -12px rgba(0,0,0,0.2); }
    .login-box { max-width: 400px; margin: 100px auto; text-align: center; }
    input, textarea { width: 100%; padding: 14px 20px; border: 1px solid #cbd5e1; border-radius: 60px; font-size: 1rem; margin-bottom: 16px; }
    button { background: #ff6b4a; color: white; border: none; padding: 14px 28px; border-radius: 60px; font-weight: bold; cursor: pointer; transition: 0.2s; }
    button:hover { background: #e5532f; transform: translateY(-2px); }
    .error { color: #dc2626; margin-top: 12px; }
    .section { border-bottom: 1px solid #e2e8f0; margin-bottom: 32px; padding-bottom: 24px; }
    .section h3 { margin-bottom: 20px; color: #ff6b4a; }
    .field { margin-bottom: 16px; }
    .field label { display: block; font-weight: 600; margin-bottom: 6px; }
    .dynamic-item { display: flex; gap: 12px; margin-bottom: 12px; align-items: center; flex-wrap: wrap; background: #f8fafc; padding: 12px; border-radius: 24px; }
    .dynamic-item input { flex: 1; margin: 0; }
    .btn-sm { padding: 8px 16px; font-size: 0.8rem; background: #e2e8f0; color: #1e2a3a; margin-left: 8px; }
    .btn-danger { background: #fee2e2; color: #dc2626; }
    .btn-success { background: #2a9d8f; margin-top: 16px; width: auto; }
    .admin-header { display: flex; justify-content: space-between; align-items: center; margin-bottom: 32px; }
    .logout-btn { background: #475569; }
  </style>
</head>
<body>
<div class="container" id="app"></div>

<script>
  const ADMIN_PASSWORD = "ocean2026"; // Смените на свой пароль
  let isLogged = false;
  let siteData = null;

  // Данные по умолчанию (такие же, как в публичном сайте)
  const defaultData = {
    hero: { badge: '<i class="fas fa-star"></i> Более 10 лет на рынке', title: 'Организуем праздники, которые объединяют', desc: 'Корпоративы, тимбилдинги, свадьбы и детские праздники — под ключ. Укрепите командный дух и создайте незабываемые моменты.', btn: 'Обсудить проект', heroImg: 'https://images.unsplash.com/photo-1511795409674-a87e9a4c590a?w=600&h=500&fit=crop' },
    stats: { s1n:'500+', s1t:'Мероприятий', s2n:'98%', s2t:'Довольных клиентов', s3n:'15+', s3t:'Команда профи' },
    services: { title:'Что мы предлагаем', subtitle:'Идеальное событие для любой задачи — от активного тимбилдинга до элегантного корпоратива', list:[ {icon:'fas fa-champagne-glasses', name:'Корпоративы', desc:'Тематические вечеринки, выездные форматы, новогодние корпоративы под ключ.'}, {icon:'fas fa-users', name:'Тимбилдинги', desc:'Спортивные квесты, верёвочные курсы, командообразование на природе.'}, {icon:'fas fa-birthday-cake', name:'Детские праздники', desc:'Аниматоры, квесты, мастер-классы и яркие шоу для детей.'}, {icon:'fas fa-ring', name:'Свадьбы', desc:'Организация выездных церемоний, стилизованных свадеб.'}, {icon:'fas fa-graduation-cap', name:'Выпускные', desc:'Школьные и садовские выпускные, вечера в стиле "Оскар".'}, {icon:'fas fa-futbol', name:'Спорт & активность', desc:'Спартакиады, велопробеги, пейнтбол.'} ] },
    gallery: { title:'Наши последние события', subtitle:'Живые кадры с корпоративов, тимбилдингов и праздников', items: [ {img:'https://images.unsplash.com/photo-1566737236504-66c99f417707?w=400&h=300&fit=crop', caption:'Корпоратив в стиле ретро'}, {img:'https://images.unsplash.com/photo-1528605248644-14dd04022da1?w=400&h=300&fit=crop', caption:'Командообразующий квест'}, {img:'https://images.unsplash.com/photo-1471107340929-a87cd0f5b5f3?w=400&h=300&fit=crop', caption:'Детский научный шоу'}, {img:'https://images.unsplash.com/photo-1519741497674-611481863552?w=400&h=300&fit=crop', caption:'Свадьба на закате'}, {img:'https://images.unsplash.com/photo-1556761175-5973dc0f32e7?w=400&h=300&fit=crop', caption:'Верёвочный курс'}, {img:'https://images.unsplash.com/photo-1533174072545-7a4b6ad7a6c3?w=400&h=300&fit=crop', caption:'Выпускной вечер'} ] },
    team: { title:'Команда профессионалов', subtitle:'Наши организаторы, ведущие и креаторы — душа каждого праздника', members: [ {name:'Анна К.', role:'Креативный продюсер', img:'https://randomuser.me/api/portraits/women/68.jpg'}, {name:'Дмитрий В.', role:'Ведущий, шоумен', img:'https://randomuser.me/api/portraits/men/32.jpg'}, {name:'Екатерина М.', role:'Тимбилдинг-коуч', img:'https://randomuser.me/api/portraits/women/44.jpg'}, {name:'Алексей П.', role:'Режиссёр праздников', img:'https://randomuser.me/api/portraits/men/45.jpg'} ] },
    contacts: { title:'Давайте создадим праздник вместе', subtitle:'Оставьте заявку — мы перезвоним и предложим лучшее решение под ваш бюджет' }
  };

  function loadData() {
    let saved = localStorage.getItem('ocean_edit_data_full');
    if(saved) {
      try { siteData = JSON.parse(saved); } catch(e) { siteData = JSON.parse(JSON.stringify(defaultData)); }
    } else {
      siteData = JSON.parse(JSON.stringify(defaultData));
    }
  }
  function saveData() { localStorage.setItem('ocean_edit_data_full', JSON.stringify(siteData)); }

  function escapeHtml(str) { return String(str).replace(/[&<>]/g, function(m){ if(m==='&') return '&amp;'; if(m==='<') return '&lt;'; if(m==='>') return '&gt;'; return m;}); }

  function renderAdminPanel() {
    const app = document.getElementById('app');
    app.innerHTML = `
      <div class="admin-panel">
        <div class="admin-header"><h2><i class="fas fa-cog"></i> Админ-панель "Океан праздников"</h2><button class="logout-btn" id="logoutBtn"><i class="fas fa-sign-out-alt"></i> Выйти</button></div>
        <div class="section"><h3>Главный блок</h3>
          <div class="field"><label>Бейдж (HTML разрешён)</label><input id="hero-badge" value="${escapeHtml(siteData.hero.badge)}"></div>
          <div class="field"><label>Заголовок</label><input id="hero-title" value="${escapeHtml(siteData.hero.title)}"></div>
          <div class="field"><label>Описание</label><textarea id="hero-desc" rows="3">${escapeHtml(siteData.hero.desc)}</textarea></div>
          <div class="field"><label>Текст кнопки</label><input id="hero-btn" value="${escapeHtml(siteData.hero.btn)}"></div>
          <div class="field"><label>URL главного фото</label><input id="hero-img" value="${siteData.hero.heroImg}"></div>
        </div>
        <div class="section"><h3>Статистика</h3>
          <div class="field"><label>Первый показатель (число)</label><input id="stat1n" value="${siteData.stats.s1n}"> <label>Текст</label><input id="stat1t" value="${escapeHtml(siteData.stats.s1t)}"></div>
          <div class="field"><label>Второй показатель</label><input id="stat2n" value="${siteData.stats.s2n}"> <label>Текст</label><input id="stat2t" value="${escapeHtml(siteData.stats.s2t)}"></div>
          <div class="field"><label>Третий показатель</label><input id="stat3n" value="${siteData.stats.s3n}"> <label>Текст</label><input id="stat3t" value="${escapeHtml(siteData.stats.s3t)}"></div>
        </div>
        <div class="section"><h3>Услуги (заголовки)</h3>
          <div class="field"><label>Заголовок блока</label><input id="services-title" value="${escapeHtml(siteData.services.title)}"></div>
          <div class="field"><label>Подзаголовок</label><input id="services-sub" value="${escapeHtml(siteData.services.subtitle)}"></div>
          <p style="font-size:14px; color:#475569;">Редактирование списка услуг (названия и описания) — в следующей версии. Сейчас можно изменить через код, но основные поля уже доступны.</p>
        </div>
        <div class="section"><h3>Галерея мероприятий</h3>
          <div class="field"><label>Заголовок галереи</label><input id="gallery-title" value="${escapeHtml(siteData.gallery.title)}"></div>
          <div class="field"><label>Подзаголовок</label><input id="gallery-sub" value="${escapeHtml(siteData.gallery.subtitle)}"></div>
          <div id="gallery-editor"></div>
          <button id="addGalleryItem" class="btn-sm"><i class="fas fa-plus"></i> Добавить фото</button>
        </div>
        <div class="section"><h3>Команда</h3>
          <div class="field"><label>Заголовок блока</label><input id="team-title" value="${escapeHtml(siteData.team.title)}"></div>
          <div class="field"><label>Подзаголовок</label><input id="team-sub" value="${escapeHtml(siteData.team.subtitle)}"></div>
          <div id="team-editor"></div>
          <button id="addTeamMember" class="btn-sm"><i class="fas fa-plus"></i> Добавить участника</button>
        </div>
        <div class="section"><h3>Контакты</h3>
          <div class="field"><label>Заголовок секции</label><input id="contact-title" value="${escapeHtml(siteData.contacts.title)}"></div>
          <div class="field"><label>Подзаголовок</label><input id="contact-sub" value="${escapeHtml(siteData.contacts.subtitle)}"></div>
        </div>
        <button id="saveAllBtn" class="btn-success"><i class="fas fa-save"></i> Сохранить все изменения</button>
      </div>
    `;
    // Динамическая галерея
    function renderGalleryEditor() {
      const container = document.getElementById('gallery-editor');
      if(!container) return;
      container.innerHTML = '';
      siteData.gallery.items.forEach((item, idx) => {
        const div = document.createElement('div');
        div.className = 'dynamic-item';
        div.innerHTML = `
          <input type="text" placeholder="URL фото" value="${escapeHtml(item.img)}" data-gallery-img="${idx}" style="flex:2">
          <input type="text" placeholder="Подпись" value="${escapeHtml(item.caption)}" data-gallery-cap="${idx}" style="flex:1">
          <button class="btn-sm btn-danger" data-del-gallery="${idx}">Удалить</button>
        `;
        container.appendChild(div);
      });
      document.querySelectorAll('[data-del-gallery]').forEach(btn => {
        btn.addEventListener('click', (e) => { let idx = parseInt(btn.getAttribute('data-del-gallery')); siteData.gallery.items.splice(idx,1); renderGalleryEditor(); });
      });
      document.querySelectorAll('[data-gallery-img]').forEach(inp => {
        inp.addEventListener('change', (e) => { let idx = parseInt(inp.getAttribute('data-gallery-img')); siteData.gallery.items[idx].img = inp.value; });
      });
      document.querySelectorAll('[data-gallery-cap]').forEach(inp => {
        inp.addEventListener('change', (e) => { let idx = parseInt(inp.getAttribute('data-gallery-cap')); siteData.gallery.items[idx].caption = inp.value; });
      });
    }
    function renderTeamEditor() {
      const container = document.getElementById('team-editor');
      if(!container) return;
      container.innerHTML = '';
      siteData.team.members.forEach((member, idx) => {
        const div = document.createElement('div');
        div.className = 'dynamic-item';
        div.innerHTML = `
          <input type="text" placeholder="Имя" value="${escapeHtml(member.name)}" data-team-name="${idx}" style="flex:1">
          <input type="text" placeholder="Роль" value="${escapeHtml(member.role)}" data-team-role="${idx}" style="flex:1">
          <input type="text" placeholder="URL фото" value="${escapeHtml(member.img)}" data-team-img="${idx}" style="flex:2">
          <button class="btn-sm btn-danger" data-del-team="${idx}">Удалить</button>
        `;
        container.appendChild(div);
      });
      document.querySelectorAll('[data-del-team]').forEach(btn => {
        btn.addEventListener('click', (e) => { let idx = parseInt(btn.getAttribute('data-del-team')); siteData.team.members.splice(idx,1); renderTeamEditor(); });
      });
      document.querySelectorAll('[data-team-name]').forEach(inp => {
        inp.addEventListener('change', (e) => { let idx = parseInt(inp.getAttribute('data-team-name')); siteData.team.members[idx].name = inp.value; });
      });
      document.querySelectorAll('[data-team-role]').forEach(inp => {
        inp.addEventListener('change', (e) => { let idx = parseInt(inp.getAttribute('data-team-role')); siteData.team.members[idx].role = inp.value; });
      });
      document.querySelectorAll('[data-team-img]').forEach(inp => {
        inp.addEventListener('change', (e) => { let idx = parseInt(inp.getAttribute('data-team-img')); siteData.team.members[idx].img = inp.value; });
      });
    }
    renderGalleryEditor();
    renderTeamEditor();
    document.getElementById('addGalleryItem').addEventListener('click', () => { siteData.gallery.items.push({img:'https://via.placeholder.com/400x300?text=Новое+событие', caption:'Новое мероприятие'}); renderGalleryEditor(); });
    document.getElementById('addTeamMember').addEventListener('click', () => { siteData.team.members.push({name:'Новый участник', role:'Должность', img:'https://randomuser.me/api/portraits/men/1.jpg'}); renderTeamEditor(); });
    document.getElementById('saveAllBtn').addEventListener('click', () => {
      // Собираем данные
      siteData.hero.badge = document.getElementById('hero-badge').value;
      siteData.hero.title = document.getElementById('hero-title').value;
      siteData.hero.desc = document.getElementById('hero-desc').value;
      siteData.hero.btn = document.getElementById('hero-btn').value;
      siteData.hero.heroImg = document.getElementById('hero-img').value;
      siteData.stats.s1n = document.getElementById('stat1n').value; siteData.stats.s1t = document.getElementById('stat1t').value;
      siteData.stats.s2n = document.getElementById('stat2n').value; siteData.stats.s2t = document.getElementById('stat2t').value;
      siteData.stats.s3n = document.getElementById('stat3n').value; siteData.stats.s3t = document.getElementById('stat3t').value;
      siteData.services.title = document.getElementById('services-title').value;
      siteData.services.subtitle = document.getElementById('services-sub').value;
      siteData.gallery.title = document.getElementById('gallery-title').value;
      siteData.gallery.subtitle = document.getElementById('gallery-sub').value;
      siteData.team.title = document.getElementById('team-title').value;
      siteData.team.subtitle = document.getElementById('team-sub').value;
      siteData.contacts.title = document.getElementById('contact-title').value;
      siteData.contacts.subtitle = document.getElementById('contact-sub').value;
      // Галерея и команда уже обновлены через инпуты
      saveData();
      alert('✅ Все изменения сохранены! Публичный сайт обновится после перезагрузки страницы.');
    });
    document.getElementById('logoutBtn').addEventListener('click', () => { localStorage.removeItem('admin_logged'); isLogged = false; renderLogin(); });
  }

  function renderLogin() {
    document.getElementById('app').innerHTML = `
      <div class="login-box">
        <h2><i class="fas fa-lock"></i> Вход в админ-панель</h2>
        <input type="password" id="password" placeholder="Введите пароль">
        <button id="loginBtn">Войти</button>
        <div id="loginError" class="error"></div>
      </div>
    `;
    document.getElementById('loginBtn').addEventListener('click', () => {
      const pwd = document.getElementById('password').value;
      if(pwd === ADMIN_PASSWORD) {
        localStorage.setItem('admin_logged', 'true');
        isLogged = true;
        loadData();
        renderAdminPanel();
      } else {
        document.getElementById('loginError').innerText = 'Неверный пароль';
      }
    });
  }

  // Проверка авторизации при загрузке
  if(localStorage.getItem('admin_logged') === 'true') {
    isLogged = true;
    loadData();
    renderAdminPanel();
  } else {
    renderLogin();
  }
</script>
</body>
</html>